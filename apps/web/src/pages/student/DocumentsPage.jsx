export function DocumentsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-content mb-1">Document Tracker</h1>
      <p className="text-content-muted text-sm">Phase 5 — per-document status + re-upload</p>
    </div>
  )
}
